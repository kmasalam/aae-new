(function ($) {
  // Whole-script strict mode syntax
  "use strict";

  // Kounty Plugin
  var pluginCheck = $(".aae-countdown__container");
  if (pluginCheck.length) {
    pluginCheck.kounty();
  }
})(jQuery);
